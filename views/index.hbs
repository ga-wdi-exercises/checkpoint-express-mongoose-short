{{!-- A `views/index.hbs` lists the most recently published Notes, **showing the `title` and `author` of each `Note`**. --}}
<h1>Hello World</h1>

<ul>
    {{#each notes}}
        <li><a href="/{{this.title}}">{{this.author}}</a></li>
</ul>